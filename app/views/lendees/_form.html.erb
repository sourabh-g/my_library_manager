<%= form_for(@lendee) do |f| %>
  <% if @lendee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lendee.errors.count, "error") %> prohibited this lendee from being saved:</h2>

      <ul>
      <% @lendee.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
